# The portal database and temporary upload chunks live here.
# Nothing in this folder should ever be served over the web.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
